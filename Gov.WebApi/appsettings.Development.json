{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Overrides appsettings.json only when ASPNETCORE_ENVIRONMENT=Development (see
  // Properties/launchSettings.json). Fill these in with your own app registration so you
  // can test locally without touching the committed production values above.
  "Authentication": {
    "TenantId": "TODO-your-tenant-id-guid",
    "ApiClientId": "TODO-your-app-registration-client-id-guid"
  },
  "PublicBaseUrl": "http://localhost:5049"
}
